const images_logo = {
    logo: require('./logotk.svg').default,
    home: require('./home.svg').default,
    close: require('./clear.svg').default,
    search: require('./search.svg').default,
    noImage: require('./noImage.png'),
};

export default images_logo;
